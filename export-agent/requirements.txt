# AI Export Facilitation Agent — Python Dependencies
# All free/open-source. Install with: pip install -r requirements.txt

# Core web framework
streamlit>=1.35.0

# Vector database (local, no server needed)
chromadb>=0.5.0

# Semantic embeddings (runs locally, no API cost)
sentence-transformers>=3.0.0

# LLM APIs (free tiers)
groq>=0.9.0
google-generativeai>=0.7.0

# Data handling
pandas>=2.0.0
openpyxl>=3.1.0          # Excel read/write

# PDF reading
pdfplumber>=0.11.0
pypdf>=4.0.0              # fallback PDF reader

# Environment / secrets
python-dotenv>=1.0.0

# Voice input (mic -> text) and text-to-speech output
# speech-recognition is used for simple fallback; primary STT uses Groq Whisper (already via groq)
# gTTS converts answers into spoken audio
gTTS>=2.5.0
